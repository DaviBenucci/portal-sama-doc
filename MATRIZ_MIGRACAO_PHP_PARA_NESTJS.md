# Matriz de Migração PHP para NestJS

Status desta matriz: auditoria em andamento baseada em `docs/MAPEAMENTO_MIGRACAO_APIS.md` e nos arquivos reais em `api/`. A fundação `portal-sama-api`, o endpoint técnico `/api-v2/health`, a base `/api-v2/auth/*`, auditoria/RBAC iniciais e parte de `DocumentsModule` foram criados; nenhum endpoint PHP foi desligado ainda.

| Endpoint PHP atual | Arquivo PHP | Novo endpoint `/api-v2` | Módulo NestJS | Status | Testes | Observações |
|---|---|---|---|---|---|---|
| `/api/storage.php?action=auth_login` | `api/storage.php` | `/api-v2/auth/login` | `AuthModule` | Parcialmente implementado | Unitário e e2e CSRF passaram | Login legado usado por `Login.js`; endpoint NestJS criado com bcrypt/JWT/refresh cookie e CSRF obrigatório, mas não conectado ao frontend nem validado contra MySQL real. |
| `/api/storage.php?action=auth_session_status` | `api/storage.php` | `/api-v2/auth/me` | `AuthModule` | Parcialmente implementado | E2E sem token passou | Sessão consultada por `auth.js`; endpoint NestJS usa bearer access JWT. |
| `/api/storage.php?action=auth_logout` | `api/storage.php` | `/api-v2/auth/logout` | `AuthModule` | Parcialmente implementado | Unitário indireto e CSRF service passaram | Logout NestJS revoga refresh token, limpa cookies e exige CSRF; fluxo real com MySQL/frontend pendente. |
| `/api/storage.php?action=auth_forgot_password` | `api/storage.php` | `/api-v2/auth/forgot-password` | `AuthModule` | Parcialmente implementado | Não executado | Resposta genérica implementada; fluxo real de recuperação/envio ainda pendente. |
| `/api/storage.php?action=get/set/delete` | `api/storage.php` | Endpoints por recurso | `ClientsModule`, `UsersModule`, `AuditModule` | Parcialmente implementado | Unitário/e2e passaram para clientes | Endpoint genérico começou a ser dividido por domínio; `ClientsModule` cobre a primeira fatia de clientes, mas KV store, colaboradores, backups e vários domínios seguem no legado. |
| Chaves/dados de clientes em storage legado | `api/storage.php`, `DEV/dev-data.js` | `/api-v2/clients`, `/api-v2/clients/:id`, `/api-v2/clients/:id/dashboard` | `ClientsModule` | Parcialmente implementado | Unitário/e2e passaram; frontend lint/build/audit passaram | CRUD inicial, dashboard, aliases legados, CSRF e auditoria implementados; `/clientes`, `/clientes/:clientId/painel`, `/clientes/visao-geral`, `/dev/clientes/novo` e `/departamentos/clientes` ja possuem telas React iniciais; falta backfill, vínculos cliente-usuário/gestor/departamento, atribuicao/transferencia de carteira, documentos completos do painel e validação real. |
| Chaves/dados de colaboradores em storage legado | `api/storage.php`, `DEV/dev-data.js` | `/api-v2/collaborators`, `/api-v2/collaborators/:id`, `/api-v2/transfers/dashboard` | `CollaboratorsModule`, `TransfersModule` | Parcialmente implementado | Unitário/e2e API passaram; frontend lint/build/audit passaram | CRUD inicial de colaboradores internos baseado em `User`, campos funcionais (`cargo`, telefone, ramal), aliases legados, permissões `collaborators.*`, CSRF, escopo por departamento e auditoria implementados. Frontend possui `/colaboradores/visao-geral`, detalhe `/clientes/colaboradores/:id`, gestao DEV `/dev/colaboradores`, cadastro DEV e `/manager/colaboradores` para consulta de carteira via `TransfersModule`; faltam backfill de `sama_colaboradores`, vínculos reais colaborador-cliente/gestor, MySQL real e Playwright. |
| `/api/storage.php?action=audit_*` | `api/storage.php` | `/api-v2/audit/logs` e correlatos | `AuditModule` | Parcialmente implementado | Unitário e e2e de proteção passaram | `GET /api-v2/audit/logs` e `/logs/:id` criados com JWT e `audit.read`; `audit_push`, lixeira, backup/restauração, retenção e integração da tela ainda pendentes. |
| `/api/storage.php?action=admin_users_list` | `api/storage.php` | `/api-v2/users`, `/api-v2/users/:id` | `UsersModule` | Parcialmente implementado | Unitário/e2e API e lint/build/audit frontend passaram | Consultas e mutações principais de usuários criadas com JWT, permissões granulares, CSRF/auditoria e sem expor `passwordHash`; `/dev` possui primeira tela React consumindo listagem, criacao, edicao, status e roles. Presença, estatísticas, migração de usuários legados e validação real seguem pendentes. |
| `/api/storage.php?action=admin_user_set_status/admin_user_stats/admin_user_stats_rollup` | `api/storage.php` | `/api-v2/users/:id/status`, `/api-v2/users/:id/stats` | `UsersModule` | Parcialmente implementado | Unitário/e2e API e lint/build/audit frontend passaram | `PATCH /api-v2/users/:id/status` foi criado com CSRF, auditoria e bloqueio de auto-inativação; `/dev` permite ativar/bloquear por permissao `users.status`. Estatísticas administrativas seguem no legado. |
| Chaves/roles administrativos em `sama_usuarios_login_v1` | `api/storage.php` | `/api-v2/roles`, `/api-v2/permissions` | `RolesModule`, `PermissionsModule` | Parcialmente implementado | Unitário/e2e API e lint/build/audit frontend passaram | Consultas e mutações principais de roles/permissões criadas com `roles.*`, `permissions.*`, CSRF e auditoria; `/dev` possui abas de roles e permissoes com criacao/edicao/troca de permissoes. Seed atualizado, MySQL real e backfill seguem pendentes. |
| `/api/storage.php?action=backup_*` | `api/storage.php` | `/api-v2/admin/backups` | `AdminModule` ou `AuditModule` | Mapeado | Não executado | Precisa proteção administrativa forte. |
| `/api/client_documents.php?action=template` | `api/client_documents.php` | `/api-v2/documents/templates` | `DocumentsModule` | Parcialmente implementado | Unitário e e2e de proteção passaram | Template de documentos obrigatórios criado em NestJS; ainda não conectado ao frontend. |
| `/api/client_documents.php?action=list` | `api/client_documents.php` | `/api-v2/clients/:clientId/documents` | `DocumentsModule` | Parcialmente implementado | Unitário/e2e de proteção passaram; frontend lint/build passaram | Exige validação de escopo do cliente; endpoint NestJS criado com JWT e `documents.read` e conectado ao painel React inicial em `ClientDocumentsPanel.tsx`; ainda falta validar contra MySQL/storage reais e escopo com dados persistidos. |
| `/api/client_documents.php?action=required_pending_summary` | `api/client_documents.php` | `/api-v2/documents/required-pending-summary` | `DocumentsModule` | Parcialmente implementado | Unitário e e2e de proteção passaram | Resumo de pendências criado em NestJS; exige `documents.requirements` e papel gerencial; validação com MySQL real pendente. |
| `/api/client_documents.php?action=add_custom` | `api/client_documents.php` | `/api-v2/documents/custom-requirements` | `DocumentsModule` | Parcialmente implementado | Unitário/e2e CSRF passaram; frontend lint/build passaram | Cria `DocumentRequirement` com `documents.requirements`, papel gerencial, CSRF e auditoria; painel React inicial ja possui formulario protegido por permissao. Migration/seed real e homologacao pendentes. |
| `/api/client_documents.php?action=upload` | `api/client_documents.php` | `/api-v2/documents/upload`, `/api-v2/documents/clients/:clientId/upload` | `DocumentsModule` | Parcialmente implementado | Unitário/e2e CSRF passaram; frontend lint/build passaram | Upload NestJS criado com JWT, `documents.upload`, CSRF, validação de extensão/MIME/assinatura/tamanho/hash, quarentena privada, scanner configuravel e storage privado; painel React inicial ja envia multipart. Falta validar ClamAV strict, MySQL/storage reais e arquivos reais. |
| `/api/client_documents.php?action=download` | `api/client_documents.php` | `/api-v2/documents/:id/download` | `DocumentsModule` | Parcialmente implementado | E2E de permissão passou; frontend lint/build passaram | Download NestJS criado com JWT, `documents.download`, stream de storage privado e auditoria; painel React inicial consome blob protegido. Risco principal segue IDOR por vínculo cliente/gestor em dados reais. |
| `/api/onboarding.php?action=docs_confirm` | `api/onboarding.php` | `/api-v2/documents/:id/status` | `DocumentsModule`, futuro `OnboardingModule` | Parcialmente implementado | Unitário/e2e de proteção passaram; frontend lint/build passaram | `PATCH /api-v2/documents/:id/status` criado com JWT, `documents.review`, CSRF, escopo por papel/departamento, metadata de revisao e auditoria. Painel React inicial ja permite revisao de documento; ainda nao substitui o fluxo `data_json` do onboarding nem foi validado em MySQL real. |
| `/api/certificados.php?action=list/create/update/delete/download` | `api/certificados.php` | `/api-v2/certificates` e `/api-v2/certificates/:id/download` | `CertificatesModule` | Parcialmente implementado | Unitário/e2e passaram | Backend criado com CRUD, download, rotação de senha, criptografia AES-GCM, validação `.p12/.pfx`, storage privado, migration `digital_certificates` e auditoria; falta MySQL/storage reais, backfill e frontend. |
| `/api/legalizacao.php?action=list/get/create/update/delete/restore/hard_delete/purge` | `api/legalizacao.php` | `/api-v2/legalization/processes`, `/api-v2/contracts` | `LegalizationModule`, `ContractsModule` | Parcialmente implementado | Unitário/e2e passaram | `ContractsModule` cobre contratos; `LegalizationModule` cobre processos internos com listagem, detalhe, criação, atualização, status/etapa, timeline e arquivamento lógico. Restore/hard_delete/purge, backfill legado e frontend seguem pendentes. |
| `/api/legalizacao.php?action=importDocument/renderPdf` | `api/legalizacao.php` | `/api-v2/contracts/:id/import`, `/api-v2/contracts/:id/render-pdf`, `/api-v2/contracts/:id/generate` | `ContractsModule` | Parcialmente implementado | Unitário/e2e parciais passaram | `POST /api-v2/contracts/:id/generate` cria snapshot HTML validado; importação PDF/Word e renderização PDF real em sandbox seguem pendentes. |
| `/api/legalizacao.php?action=getByToken/clientSubmit` | `api/legalizacao.php` | `/api-v2/public/signatures/:token`, `/api-v2/public/signatures/:token/sign` | `ContractsModule`, `SignaturesModule`, `PublicLinksModule` | Parcialmente implementado | Unitário/e2e parciais passaram | GET/POST público por token opaco implementado com hash, expiração, revogação, escopo, throttle e auditoria; compatibilidade com tokens legados e frontend público seguem pendentes. |
| `/api/legalizacao.php?action=listTemplates/createTemplate/updateTemplate/deleteTemplate` | `api/legalizacao.php` | `/api-v2/legalization/templates` | `LegalizationModule` | Parcialmente implementado | Unitário/e2e passaram | CRUD de templates de cabecalho/rodape criado com `legalization.read` para leitura, `legalization.templates` nas mutacoes, CSRF, auditoria, soft delete e rejeicao de HTML inseguro; falta backfill legado e integracao frontend. |
| `/api/propostas.php?action=list/get/create/save/send/delete` | `api/propostas.php` | `/api-v2/proposals`, `/api-v2/proposals/:id/send`, `/api-v2/proposals/:id/approve`, `/api-v2/proposals/:id/reject` | `ProposalsModule` | Parcialmente implementado | Unitário/e2e passaram | CRUD inicial, envio por token público opaco, aliases legados de resposta, CSRF e auditoria implementados; delete/backfill/tela e MySQL real seguem pendentes. |
| `/api/propostas.php?action=client_get/client_action` | `api/propostas.php` | `/api-v2/public/proposals/:token` | `ProposalsModule`, `PublicLinksModule` | Parcialmente implementado | Unitário/e2e parciais passaram | GET/POST público por token opaco implementado com hash, expiração, revogação, escopo, throttle e auditoria; compatibilidade com tokens legados e frontend público seguem pendentes. |
| `/api/onboarding.php?action=list/create/get/delete/move/stage_set/finish` | `api/onboarding.php` | `/api-v2/onboarding/processes` | `OnboardingModule` | Parcialmente implementado | Unitário/e2e passaram | `OnboardingModule` cobre listagem, detalhe, criação, atualização, status/etapa, timeline e arquivamento lógico com JWT, permissões `onboarding.*`, CSRF, escopo por responsável/criador/departamento e auditoria; máquina de estados completa, notificações, finish real e backfill seguem pendentes. |
| `/api/onboarding.php?action=proposal_save/proposal_send/client_get/client_action` | `api/onboarding.php` | `/api-v2/onboarding/processes/:id/proposal`, `/api-v2/public/onboarding/proposals/:token` | `OnboardingModule`, `ProposalsModule` | Mapeado | Não executado | Fluxo de proposta no onboarding. |
| `/api/onboarding.php?action=docs_get/docs_link/docs_client_get/docs_upload/docs_download/docs_confirm/docs_finish` | `api/onboarding.php` | `/api-v2/onboarding/processes/:id/documents`, `/api-v2/public/onboarding/documents/:token`, `/api-v2/documents/public-upload?token=...` | `OnboardingModule`, `DocumentsModule`, `PublicLinksModule` | Parcialmente implementado | Unitário/e2e API parciais e lint/build/audit frontend passaram | `OnboardingModule` registra estado/requisitos/documentos em metadata do processo; `DocumentsModule` cobre upload interno, upload público por `PublicToken`, gestão administrativa de tokens, download e revisão de status. Em 2026-05-18, `/onboarding/publico/documentos/:token` ganhou primeira tela React de upload publico. Checklist publico especifico, docs_finish, MySQL/storage reais e Playwright seguem pendentes. |
| `/api/access_requests.php?action=list/gestor_approvals/colaborador_status/decision` | `api/access_requests.php` | `/api-v2/access-requests`, `/api-v2/access-requests/my/latest`, `/api-v2/access-requests/manager/approvals`, `/api-v2/access-requests/:id/approve`, `/api-v2/access-requests/:id/reject` | `AccessRequestsModule`, `TiModule` | Parcialmente implementado | Unitário/e2e passaram; frontend lint/build/audit passaram | Backend criado com listagem, detalhe, ultima solicitacao do colaborador, fila do gestor, aprovacao/rejeicao, escopo por gestor/departamento/TI, CSRF, auditoria e emissores automaticos para `NotificationsModule`; `/solicitacao-acesso` e `/ti/acessos` agora possuem telas React iniciais consumindo esses contratos. Falta backfill, validacao com dados reais, Playwright e cofre seguro para credenciais TI. |
| `SolicitacaoAcesso/enviar_acesso.php` | `SolicitacaoAcesso/enviar_acesso.php` | `/api-v2/access-requests` | `AccessRequestsModule` | Parcialmente implementado; legado endurecido | Unitário/e2e passaram; frontend lint/build/audit passaram; PHP lint bloqueado | `POST /api-v2/access-requests` cria solicitacao de colaborador ou gestor com validacao server-side de datas/horarios e aliases legados de resposta; `display_errors` foi desabilitado por padrão no legado em 2026-05-08. A tela React inicial envia pelo service `access-requests.service.ts` com CSRF via client centralizado. |
| `/api/manager_workspace.php?action=overview/history_*` | `api/manager_workspace.php` | `/api-v2/managers/overview`, `/api-v2/managers/history`, `/api-v2/managers/history/:companyId/timeline`, `POST /api-v2/managers/history`, `PATCH /api-v2/managers/history/:id`, `POST /api-v2/managers/company-life` | `ManagersModule` | Parcialmente implementado | Backend/frontend lint/build e Prisma validate passaram | Overview, leitura de historico e vida da empresa criados com JWT, RBAC `manager_history.read`, escopo por ADMIN/DEV/MANAGER + departamento e modelos Prisma `UserPresence`, `CompanyHistoryEntry`/`CompanyLifeEntry`; mutacoes iniciais substituem `history_append`, `history_update` e `company_life_save` com `manager_history.write`, CSRF e auditoria. MySQL real, migrations/seed em ambiente, usuarios reais e Playwright seguem pendentes antes de desligar o legado. |
| `/api/manager_workspace.php?action=calendar_*` | `api/manager_workspace.php` | `/api-v2/calendar`, `/api-v2/calendar/entries/:id` | `CalendarModule` | Parcialmente implementado | Backend/frontend lint/build, Prisma validate e unitario focado passaram | `GET/POST /api-v2/calendar/config`, `GET /api-v2/calendar/month`, `GET /api-v2/calendar/day` e `DELETE /api-v2/calendar/entries/:id` cobrem configuracao, leitura mensal/diaria e exclusao de vencimento com JWT, RBAC `calendar.read/manage`, CSRF em mutacoes, escopo por departamento e auditoria. `/manager/calendario/config` configura regras e `/manager` lista/remove vencimentos. Faltam MySQL real, seed atualizado, dados de calendario/carteira, Playwright e desligamento controlado do PHP legado. |
| `/api/manager_workspace.php?action=transfer_*` | `api/manager_workspace.php` | `/api-v2/transfers`, `/api-v2/transfers/dashboard`, `/api-v2/transfers/:id/return` | `TransfersModule` | Parcialmente implementado | Backend: Prisma validate/build/lint/unitarios e suite completa passaram; frontend: lint/build/audit passaram | Dashboard, criacao de sessao e retorno manual criados com JWT, RBAC `transfers.*`, CSRF em mutacoes, auditoria, escopo por departamento e `TransferSession` mapeado para `sama_transfer_sessions`; `/manager/transferencias` opera transferencias e `/manager/colaboradores` consulta carteiras por colaborador consumindo esses contratos. Faltam MySQL real, seed atualizado, backfill/modelagem formal de carteira, usuarios reais e Playwright. |
| `/api/integra_ai.php?action=search_companies/parse_bank_statement/create_job/load_job/save_step2/save_step3/list_step4_rules/save_step4_rule/generate_txt/download_txt` | `api/integra_ai.php` | `/api-v2/accounting/integra-ai/workspaces`, `/api-v2/accounting/integra-ai/jobs/:id` e futuras mutacoes `/import`, `/reconcile`, `/export` | `AccountingModule`, `IntegraAiModule`, `JobsModule` | Implementado parcialmente | Unitario focado/RBAC/lint/build API e lint/build frontend passaram | Primeira fatia read-only criada com JWT, RBAC `accounting.integra_ai.read`, escopo contabil, leitura parametrizada das tabelas `sama_integra_ai_*`, sanitizacao de `source_path`/`txt_path` e auditoria ao abrir job. Actions de upload/parser/salvamento de etapas/regras/geracao/download TXT seguem no PHP ate existir camada segura de mutacoes, upload e exportacao. |
| `/api/notifications.php?action=list/create/ack/alert_ack/clear_unread` + `/api/notifications_stream.php` | `api/notifications.php`, `api/notifications_stream.php` | `/api-v2/notifications`, `/api-v2/notifications/stream`, `/api-v2/notifications/push/*`, `/api-v2/notifications/:id/ack`, `/api-v2/notifications/:id/alert-ack`, `/api-v2/notifications/clear-unread` | `NotificationsModule` | Parcialmente implementado | Unitário/e2e passaram | Listagem, criacao, SSE com `last_id`/`lastId` e catch-up por MySQL, Web Push com subscriptions VAPID, ack, alert-ack e limpeza de nao lidas criadas com escopo por usuario/departamento, CSRF nas mutacoes, auditoria e RBAC `notifications.read/create`; primeiro emissor automatico foi ligado a `AccessRequestsModule`; faltam demais emissores, VAPID real, teste browser/homologacao e frontend React. |
| `/api/fiscal_workspace.php?action=load_board/cycle_cell/set_cell_status` | `api/fiscal_workspace.php` | `/api-v2/departments/fiscal/workspace`, `/cycle-cell`, `/cell-status` | `DepartmentsModule` | Implementado parcialmente | Unitario focado, lint/build API e lint/build frontend passaram | Workspace fiscal/departamental inicial criado com leitura da grade, colunas/status do legado, vencimentos vinculados via calendario, mutacoes com CSRF/RBAC/auditoria e persistencia em `Client.metadata.fiscalSheet`; falta validar MySQL/homologacao, seed `departments.workspace.*`, backfill de responsaveis fiscais, Playwright e desligamento controlado do PHP legado. |
